#include <stdio.h>
        int main()
            {
                int a,b;
                printf("Enter 1st Number:");
              scanf("%d",&a);

              printf("\nEnter 2nd Number :");
              scanf("%d",&b);



              if (a>b)
              {
                printf("%d is larger than %d",a,b);

              }

               else
              {
                printf("%d is larger than %d",b,a);
              }